const mineflayer = require("mineflayer");

const options = {
  host: "localhost", // Change this to the ip you want.
  port: 19132, // Change this to the port you want.
};

const bot = mineflayer.createBot(options);
